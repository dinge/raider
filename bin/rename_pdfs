#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
$LOAD_PATH.unshift File.expand_path('../lib', __dir__)
require 'rename_pdfs'
require 'config'

config = Raider::Config.from_args
Raider::RenamePdfs.new(config).run