# frozen_string_literal: true

RSpec.describe Raider do
  it "has a version number" do
    expect(Raider::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
